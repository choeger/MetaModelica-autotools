# variables for PCC / Darwin 7.8.0 / gcc
# Optimizations should be safe for all PPCs. -O2 should
# include -fomit-frame-pointer...
CC=gcc
CFLAGS=
COFLAGS=-O2 -pipe -mmultiple -mstring
CPFLAGS=-fno-omit-frame-pointer -pg
CGFLAGS=-g -DRML_DEBUG
RANLIB=ranlib
CPP=gcc -x c -E -ansi
AS=as
LD=ld
